[mysqld]
server-id=2                   # Unique ID for the replica server in the replication cluster
relay-log=mysql-relay-bin     # Enable relay log for replication
read_only=1                   # Ensure the replica is read-only
